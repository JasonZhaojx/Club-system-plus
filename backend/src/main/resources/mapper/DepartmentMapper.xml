<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.backend.sever.mapper.DepartmentMapper">
    <select id="countByName" resultType="int">
        select count(1)
        from department
        where name = #{name}
        <if test="excludeId != null">
            and id != #{excludeId}
        </if>
    </select>

    <update id="updateDepartment">
        update department
        set name = #{name},
            description = #{description}
        where id = #{id}
    </update>

    <update id="updateStatus">
        update department
        set status = #{status}
        where id = #{id}
    </update>
</mapper>
